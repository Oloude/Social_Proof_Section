import SocialProof from "./components/SocialProof"

export default function App() {
  return (
   <main className="font-League_Spartan min-h-screen bg-bgMobile bg-bgPosition bg-no-repeat p-6 flex justify-center items-center lg:bg-bgDesktop bg-white">
    <SocialProof/>
   </main>
  )
}